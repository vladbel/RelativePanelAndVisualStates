<Page
    x:Class="RelativePanelAndVisualStates.Page2"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RelativePanelAndVisualStates"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style TargetType="TextBlock" x:Key="HubconnectInstructionsStyle">
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="Foreground" Value="Purple"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="NextInstructionsStyle">
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="Foreground" Value="Blue"/>
        </Style>
    </Page.Resources>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="Header"
                   Grid.Row="0"
                   FontSize="30"
                   Foreground="Coral">
            Header Placeholder
        </TextBlock>
        <RelativePanel x:Name="RootPanel"
                       Grid.Row="1">
            <Border x:Name="MediaElementContainer"
                    local:MediaElementContainerHelper.Container="{Binding ElementName=RootGrid, Mode=OneWay}">
                <MediaElement x:Name="MediaElement"
                              IsLooping="True"
                              Source="/Assets/Video/gse_hub_v2.mp4" />
            </Border>
            <StackPanel x:Name="InstructionsContainer">
                <TextBlock x:Name="HubConnectInstructions"
                       Style="{StaticResource HubconnectInstructionsStyle}">
                Hub Connect Instructions
                </TextBlock>
                <TextBlock x:Name="NextInstructions"
                       Style="{StaticResource NextInstructionsStyle}">
                Next Instructions
                </TextBlock>
            </StackPanel>

        </RelativePanel>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="720"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="InstructionsContainer.(RelativePanel.RightOf)" Value="MediaElementContainer" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="InstructionsContainer.(RelativePanel.AlignBottomWith)" Value="MediaElementContainer" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>

</Page>

